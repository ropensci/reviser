% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-methods.R
\name{revision_model}
\alias{revision_model}
\title{Fitted Revision Models}
\value{
This topic documents a class rather than a function. \code{\link[=kk_nowcast]{kk_nowcast()}}
returns a list of the components described above with class attribute
\code{c("kk_model", "revision_model", "list")}, and \code{\link[=jvn_nowcast]{jvn_nowcast()}} returns the
same with \code{"jvn_model"} in place of \code{"kk_model"}.
}
\description{
\code{reviser} represents every fitted revision-nowcasting model as an S3 object
that inherits from the common parent class \code{revision_model}. The two
concrete classes are \code{\link[=kk_nowcast]{kk_nowcast()}}, which returns a \code{kk_model}, and
\code{\link[=jvn_nowcast]{jvn_nowcast()}}, which returns a \code{jvn_model}; both carry
\code{c("<family>_model", "revision_model", "list")} as their class attribute.

The parent class holds everything the two families share. The standard
extractor generics \code{\link[=coef]{coef()}}, \code{\link[=vcov]{vcov()}}, \code{\link[=logLik]{logLik()}}, \code{\link[=nobs]{nobs()}}, \code{\link[=fitted]{fitted()}},
\code{\link[=residuals]{residuals()}}, \code{\link[=predict]{predict()}} and the \code{reviser} generic \code{\link[=states]{states()}}, together
with \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}} and \code{\link[=plot]{plot()}}, are defined once for
\code{revision_model} and inherited by both families. Only the handful of
behaviors that genuinely differ between the families are dispatched
separately, through the internal generics \code{model_family()}, \code{spec_lines()},
\code{signal_state()}, \code{target_column()} and \code{default_plot_state()}.

A fitted object is a list with at least the components \code{params} (a data
frame with columns \code{Parameter}, \code{Estimate} and \code{Std.Error}), \code{states} (a
long tibble of state estimates, or \code{NULL} when the model was fitted with
\code{return_states = FALSE}), \code{loglik}, \code{n_param}, \code{n_ic}, \code{cov} and \code{data}.
A new model family becomes a full citizen of this system by returning an
object with those components, prepending \code{"revision_model"} to its class
attribute, and supplying methods for the five internal generics above.
}
\examples{
df <- get_nth_release(
  tsbox::ts_span(
    tsbox::ts_pc(dplyr::filter(reviser::gdp, id == "US")),
    start = "1980-01-01"
  ),
  n = 0:1
)
df <- na.omit(dplyr::select(df, -c("id", "pub_date")))
fit <- kk_nowcast(df, e = 1, model = "KK", method = "OLS")

# The fitted object carries the shared parent class.
class(fit)
inherits(fit, "revision_model")

# The extractor generics are inherited from that parent.
head(coef(fit))
head(states(fit))

}
\seealso{
\code{\link[=kk_nowcast]{kk_nowcast()}}, \code{\link[=jvn_nowcast]{jvn_nowcast()}}, \code{\link[=states]{states()}}

Other revision nowcasting: 
\code{\link{coef.revision_model}()},
\code{\link{fitted.revision_model}()},
\code{\link{jvn_nowcast}()},
\code{\link{kk_nowcast}()},
\code{\link{logLik.revision_model}()},
\code{\link{nobs.revision_model}()},
\code{\link{plot.revision_model}()},
\code{\link{predict.revision_model}()},
\code{\link{print.revision_model}()},
\code{\link{residuals.revision_model}()},
\code{\link{states}()},
\code{\link{summary.revision_model}()},
\code{\link{vcov.revision_model}()}
}
\concept{revision nowcasting}
