% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/revisions.R
\name{get_nth_release}
\alias{get_nth_release}
\title{Extract the Nth Data Release (Vintage)}
\usage{
get_nth_release(df, n = 0, diagonal = FALSE)
}
\arguments{
\item{df}{A data frame containing data vintages. The data frame must
include the columns:
\itemize{
\item \code{pub_date} (publication date of the release)
\item \code{time} (thecorresponding time period for the data).
}}

\item{n}{The release number to extract. Accepts:
\itemize{
\item Non-negative integer or vector
(e.g., 0 for first release, 1 for second, etc.)
\item \code{"first"} to extract the first release.
\item \code{"latest"} to extract the most recent release.
Default is 0 (the first release).
}}

\item{diagonal}{Logical. If \code{TRUE}, the function only returns real
first releases.}
}
\value{
A filtered data frame containing only the specified release(s).
The resulting data frame is assigned the class \code{tbl_release} to indicate
its structure. If diagonal is set to \code{TRUE}, the function only returns the
real first releases. That is historic values for which no vintages exist
are not returned.
}
\description{
Filters the input dataset to return the Nth release (or vintage) of data
for each time period. The function supports selecting the first, latest, or
a specific numbered release.
}
\details{
The behavior depends on the value of \code{n}:
\itemize{
\item \strong{Non-negative integer}: The function retrieves the Nth release for each
time period (e.g., 0 = first release, 1 = second release, etc.).
\item \strong{"first"}: Retrieves the first release for each time
period (via \code{get_first_release}).
\item \strong{"latest"}: Retrieves the most recent release for each time
period (via \code{get_latest_release}).
}
}
\examples{
# Example data
df <- dplyr::filter(reviser::gdp, id == "US")

# Get the first release (n = 0)
first_release <- get_nth_release(df, n = 0)

# Get the latest release
latest_release <- get_nth_release(df, n = "latest")

# Get the second release (n = 1)
second_release <- get_nth_release(df, n = 1)

# Get the first and second release (n = 0:1)
releases <- get_nth_release(df, n = 0:1)

}
\seealso{
Other revision utilities: 
\code{\link{get_days_to_release}()},
\code{\link{get_first_release}()},
\code{\link{get_fixed_release}()},
\code{\link{get_latest_release}()},
\code{\link{get_releases_by_date}()},
\code{\link{get_revisions}()}
}
\concept{revision utilities}
