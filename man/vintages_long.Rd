% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{vintages_long}
\alias{vintages_long}
\title{Convert Vintages Data to Long Format}
\usage{
vintages_long(df, names_to = "pub_date", keep_na = FALSE)
}
\arguments{
\item{df}{A data frame, tibble, or list of data frames containing vintages
data in wide format.}

\item{names_to}{The name of the column to create from the wide-format
column names. Must be either \code{"pub_date"} (default) or \code{"release"}.}

\item{keep_na}{Logical. If \code{TRUE}, retains rows with \code{NA} values in the
\code{value} column. Default is \code{FALSE}.}
}
\value{
A long-format data frame or tibble. If the input is a list of
wide-format data frames, the output will be a single combined long-format
data frame.
}
\description{
Converts a vintages dataset from wide format to long format, optionally
adding \code{id} if the input is a list of data frames. The long format contains
one row per combination of \code{time} and \code{names_to} (e.g., \code{pub_date} or
\code{release}), with values stored in a single \code{value} column.
}
\examples{
# Example wide-format data
long_data <- dplyr::filter(reviser::gdp, id == "US")

# Convert to wide format
wide_data <- vintages_wide(long_data)

# Example list of wide-format data frames
wide_list <- list(
  A = wide_data$US,
  B = wide_data$US
)

# Convert list to long format
long_data <- vintages_long(wide_list, names_to = "pub_date")

}
\seealso{
Other helpers: 
\code{\link{print.tbl_pubdate}()},
\code{\link{print.tbl_release}()},
\code{\link{summary.tbl_pubdate}()},
\code{\link{summary.tbl_release}()},
\code{\link{tbl_sum.tbl_pubdate}()},
\code{\link{tbl_sum.tbl_release}()},
\code{\link{validate_vintages}()},
\code{\link{vintages_wide}()}
}
\concept{helpers}
