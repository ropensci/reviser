% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{vintages_wide}
\alias{vintages_wide}
\title{Convert Vintages Data to Wide Format}
\usage{
vintages_wide(df, names_from = "pub_date")
}
\arguments{
\item{df}{A data frame or tibble containing vintages data in long format.}

\item{names_from}{The name of the column whose unique values will be used
as column names in the wide format. Defaults to \code{"pub_date"}.
Other: \code{"release"}.}
}
\value{
If an \code{id} column is present, the function returns a named list of
wide-format data frames, one for each unique \code{id}. Otherwise, it returns a
single wide-format data frame.
}
\description{
Converts a vintages dataset from long format to wide format, optionally
grouping by \code{id} if present. The wide format uses one column per unique
value of the \code{names_from} parameter, with observation dates (\code{time}) as
rows and values (\code{value}) as cell contents.
}
\examples{
# Example wide-format data
long_data <- dplyr::filter(reviser::gdp, id == "US")

# Convert to wide format
wide_data <- vintages_wide(long_data)

# Example list of wide-format data frames
wide_list <- list(
  A = wide_data$US,
  B = wide_data$US
)

# Convert list to long format
long_data1 <- vintages_long(wide_data, names_to = "pub_date")
long_data2 <- vintages_long(wide_list, names_to = "pub_date")

}
\seealso{
Other helpers: 
\code{\link{print.tbl_pubdate}()},
\code{\link{print.tbl_release}()},
\code{\link{reviser-vintages-classes}},
\code{\link{summary.tbl_pubdate}()},
\code{\link{summary.tbl_release}()},
\code{\link{tbl_sum.tbl_pubdate}()},
\code{\link{tbl_sum.tbl_release}()},
\code{\link{validate_vintages}()},
\code{\link{vintages_long}()}
}
\concept{helpers}
