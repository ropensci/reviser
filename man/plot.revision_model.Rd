% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphs.R
\name{plot.revision_model}
\alias{plot.revision_model}
\title{Plot Revision Model Results}
\usage{
\method{plot}{revision_model}(x, state = NULL, type = "filtered", ...)
}
\arguments{
\item{x}{A fitted model object inheriting from \link{revision_model}, such as a
\code{kk_model} or a \code{jvn_model}.}

\item{state}{String. The name of the state to visualize. If \code{NULL}, the
family's default state is used: the latent true value for a \code{jvn_model},
and the first available state for a \code{kk_model}.}

\item{type}{String. Type of estimate: "filtered" or "smoothed".}

\item{...}{Additional arguments passed to theme_reviser.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot filtered or smoothed estimates for a selected state from a fitted
revision model. Defined once for the parent class \link{revision_model} and
inherited by \code{kk_model} and \code{jvn_model} objects alike; the state shown when
\code{state} is not given is chosen by the concrete class.
}
\details{
This method requires the state estimates to be available. A model
fitted with \code{solver_options$return_states = FALSE} did not retain them,
and plotting it fails with a message naming that option, in the same way
\code{\link[=states]{states()}}, \code{\link[=fitted]{fitted()}}, \code{\link[=residuals]{residuals()}} and \code{\link[=predict]{predict()}} do.
}
\examples{
df <- get_nth_release(
  tsbox::ts_span(
    tsbox::ts_pc(
      dplyr::filter(reviser::gdp, id == "US")
    ),
    start = "1980-01-01"
  ),
  n = 0:1
)
df <- dplyr::select(df, -c("id", "pub_date"))
df <- na.omit(df)

e <- 1 # Number of efficient release
h <- 2 # Forecast horizon
result <- kk_nowcast(df, e, h = h, model = "Kishor-Koenig")

plot(result)

}
\seealso{
Other revision nowcasting: 
\code{\link{coef.revision_model}()},
\code{\link{fitted.revision_model}()},
\code{\link{jvn_nowcast}()},
\code{\link{kk_nowcast}()},
\code{\link{logLik.revision_model}()},
\code{\link{nobs.revision_model}()},
\code{\link{predict.revision_model}()},
\code{\link{print.revision_model}()},
\code{\link{residuals.revision_model}()},
\code{\link{revision_model}},
\code{\link{states}()},
\code{\link{summary.revision_model}()},
\code{\link{vcov.revision_model}()}
}
\concept{revision nowcasting}
